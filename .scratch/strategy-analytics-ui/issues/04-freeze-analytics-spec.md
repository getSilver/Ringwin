# 冻结策略分析界面规格

Type: task
Status: closed
Assignee: ox-alpha (本会话)
Label: wayfinder:task
Blocked by: 02, 03

## Question

把已决材料组装成目的地要求的冻结规格文档 `spec.md`（放本地图目录根）：

1. **IA 章节**：以 02 号原型冻结结论为准（页面清单、导航、关键交互）。
2. **数据契约章节**：以 03 号产出为准（投影扩展字段表 + 派生数据集 schema +
   归因映射与失效条件），并入 01 号图表选型结论作为前端技术约束。
3. **核心依赖与边界章节**：显式列出本规格**不**要求的核心变更（零 codec 变更
   前提、期权占位、单账户 fixture），及每项的解锁条件。
4. **验收标准草案**：未来实现波次收尾时自动验收应断言什么（如：派生数据集
   重算幂等、投影字段与重放一致性、决策链关联完整性）。
5. 规格头部标注版本（v1）、日期、来源地图链接。

冻结后更新地图：Decisions so far 补齐、fog 复查（是否有可毕业项）。

## Answer

已冻结（2026-08-26）。规格文档：[spec.md](../spec.md)（v1）。

组装来源：
- **IA 章节** ← 02 号冻结结论（A 版侧栏工作台五区 + 8 项必要 UI 映射表）
- **数据契约章节** ← 03 号产出（投影扩展字段表、九数据集、归因模型与失效条件、快照存储布局）+ 01 号图表选型（uPlot vendored 作前端技术约束）
- **核心依赖与边界**：唯一要求的核心变更 = economics.Projection 减仓观测输出；五项"不要求"边界各带解锁条件
- **验收标准草案**：七条断言（派生幂等、投影一致、决策链完整性、round-trip 守恒、快照复现、降级行为、既有回归）

fog 复查：无可毕业项——外部基准导入与 stale 价格回退仍缺真实需求驱动。

[Service]
# Only the execution-gateway role may open a venue network family. The
# binary still owns the private VenueAdapter seam and all other roles remain
# Unix-domain-socket-only under the base unit.
RestrictAddressFamilies=AF_UNIX AF_INET AF_INET6
